<h1> Appearance Form </h1> 

<% if @appearance.errors.any? %>
<% @appearance.errors.full_message.each do |error_msg| %>
    <li><%= error_msg %>
    <% end %>
<% end %>

<br>
<%= form_with model: @appearance do |f| %>
Select a guest: <%= f.collection_select :guest_id, Guest.all, :id, :name %>
<br>
Select an episode: <%= f.collection_select :episode_id, Episode.all, :id, :number%>
<br>
Enter a rating: <%= f.number_field :number%>
<br>
<%= f.submit %>
<% end %>